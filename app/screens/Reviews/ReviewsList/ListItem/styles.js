import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 300,
    borderWidth: 1,
    borderRadius: 10,
    margin: 10
  }
});

export default styles;
