import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    flex: 1
  },
  loading: {
    top: '50%'
  },
  tagsList: {
    width: '100%'
  },
  containerList: {
    paddingTop: 5,
    paddingBottom: 100
  }
});

export default styles;
