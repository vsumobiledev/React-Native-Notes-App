import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    flex: 1
  },
  loading: {
    top: '50%'
  },
  tagsList: {
    marginTop: 10,
    width: '100%'
  }
});

export default styles;
